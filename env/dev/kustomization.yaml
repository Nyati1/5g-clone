apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

configMapGenerator:
- name: open5gs-config
  behavior: merge
  literals:
    - mcc="2000"
    - mnc="204"
replicas:
  - name : open5gs-amf  
    count: 3
